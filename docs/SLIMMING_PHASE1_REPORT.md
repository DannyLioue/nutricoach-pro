# 项目瘦身报告（Phase 1）

日期：2026-03-08  
范围：低风险与中风险瘦身（不改变核心业务流程）

## 目标与结果

- 目标 1：清理无业务价值或无引用文件  
  - 结果：完成
- 目标 2：清理未使用运行时依赖  
  - 结果：完成
- 目标 3：收敛未使用 value exports（导出面治理）  
  - 结果：完成
- 目标 4：保持可构建状态  
  - 结果：完成（`npm run build` 通过）

## 关键指标

- 追踪文件数（`rg --files`）：`254`
- Git 对象目录体量（`.git`）：`102M`
- `knip` 演进：
  - `Unused files`: `26 -> 0`
  - `Unused dependencies`: `1 -> 0`
  - `Unused exports`（值导出）: `40 -> 0`
  - 当前主要剩余：`Unused exported types`

## 提交清单（按时间顺序）

1. `8131cba` 删除根目录生成测试产物并补充 `.gitignore`
2. `b525707` 删除无引用 PDF 调试脚本
3. `4707b54` 删除无引用本地测试脚本
4. `c29472a` 修复构建阻断（运动调整导出与空值类型）
5. `75356ab` 删除无引用 UI/barrel 模块
6. `0868623` 删除无引用脚本与 legacy AI 模块
7. `830ae58` 删除未使用运行时依赖
8. `38cca63` 第一批导出面清理（barrel/UI）
9. `df95202` 第二批导出面清理（内部工具导出收敛）
10. `384cea6` 第三批导出面清理（weekly/libs）
11. `377c651` 第四批导出面清理（剩余 value exports 收敛）

## 验证记录

- 构建验证：多轮执行 `npm run build`，最终持续通过
- 测试说明：
  - 全量 `npm run test` 仍存在历史失败用例（与本次瘦身主线无直接对应关系）
  - 本阶段以“构建可用 + 逐批小步提交可回滚”为准入标准

## 风险与回滚

- 风险控制策略：
  - 逐批删除，单批提交，单批构建验证
  - 优先“取消导出”而非“删实现”减少误伤概率
- 回滚策略：
  - 任意批次可通过单提交回滚（`git revert <commit>`）

## Phase 2 建议

- 聚焦 `Unused exported types` 治理（类型出口规范化）
- 对外公共类型与内部实现类型分层（`public types` / `internal types`）
- 在 `knip` 配置中明确可接受类型导出白名单，降低噪音
